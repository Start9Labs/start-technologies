name: 📡 StartWRT — Bug Report
description: A defect in StartWRT — the OpenWrt-based router OS for the SpaceMiT K1.
type: Bug
labels: [StartWRT]
<<<<<<< HEAD
body:
=======
assignees:
  - dominion5254
body:
<<<<<<< HEAD
  - type: markdown
    attributes:
      value: |
        **Filing this with an agent?** `gh issue create` bypasses this form entirely — pass the metadata yourself with `--type Bug --label StartWRT`, and mirror the `###` headings below as your body's sections. Title the issue as the *finding*, not the symptom alone: `<what breaks> — <why> (<file:line>)`.

>>>>>>> 149a886f6 (chore(github): per-project bug forms, native issue types, agent filing contract)
=======
>>>>>>> 745136b51 (chore(github): drop agent-addressed preambles the web form can't deliver)
  - type: checkboxes
    id: prerequisites
    attributes:
      label: Prerequisites
      options:
        - label: I searched [existing issues](https://github.com/Start9Labs/start-technologies/issues?q=is%3Aissue) and this is not already reported.
          required: true

  - type: input
    id: version
    attributes:
      label: StartWRT version / image
      description: The release you flashed. If you built the image yourself, give the git hash instead.
      placeholder: 1.0.0-beta.3
    validations:
      required: true

  - type: dropdown
    id: board
    attributes:
      label: Board
      options:
        - SpaceMiT K1 (BananaPi-F3)
        - Other / built for a different target
      default: 0
    validations:
      required: true

  - type: dropdown
    id: area
    attributes:
      label: Which surface?
      options:
        - Flashing / first boot
        - Web UI
        - startwrt RPC or CLI
        - Network configuration (UCI)
        - WiFi
        - Routing & firewall
        - DNS / DHCP
        - Upgrade
        - OpenWrt base layer
        - Other / unsure
    validations:
      required: true

  - type: textarea
    id: current
    attributes:
      label: What happens
    validations:
      required: true

  - type: textarea
    id: expected
    attributes:
      label: What should happen instead
    validations:
      required: true

  - type: textarea
    id: repro
    attributes:
      label: Steps to reproduce
<<<<<<< HEAD
      description: Number them, and say what you saw at the step where it went wrong.
=======
      description: If you found this by reading the code rather than hitting it, leave this blank and fill in **Root cause** below instead.
>>>>>>> 149a886f6 (chore(github): per-project bug forms, native issue types, agent filing contract)

  - type: textarea
    id: uci
    attributes:
      label: Relevant UCI config
      description: '`uci show <package>` for whichever config is involved — e.g. `uci show network`, `uci show firewall`. Redact keys and passwords.'
      render: shell

  - type: textarea
<<<<<<< HEAD
=======
    id: root-cause
    attributes:
      label: Root cause / code pointers
      description: If you traced it, name the file, the line, and the mechanism. Leave it blank rather than guessing — an unverified cause sends triage down the wrong path. StartWRT's own backend is `projects/start-wrt/` (crates `ctrl`, `uciedit`); changes to the OpenWrt base live in `openwrt-patches/` and `openwrt-overlay/`, never in the disposable `openwrt/` build workspace.
      placeholder: |
        `<path/to/file>:<line>` — <what the code does today, and why that is wrong>

  - type: textarea
>>>>>>> 149a886f6 (chore(github): per-project bug forms, native issue types, agent filing contract)
    id: logs
    attributes:
      label: Logs & evidence
      description: '`logread`, plus `dmesg` for anything at the kernel or driver layer.'
      render: shell
